<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="350dp"
    android:layout_margin="16dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="6dp"
    android:background="@android:color/white"
    app:strokeColor="#33000000"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@android:color/white">

        <!-- Top Row: Info icon + Creator -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageButton
                android:id="@+id/infoButton"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_info"
                android:tint="#444"
                android:layout_alignParentStart="true"
                android:contentDescription="More Info" />

            <LinearLayout
                android:id="@+id/creatorInfoLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <LinearLayout
                    android:orientation="vertical"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp">

                    <TextView
                        android:id="@+id/projectCreatorName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Alex Johnson"
                        android:textColor="#000000"
                        android:textStyle="bold"
                        android:textSize="14sp" />

                    <TextView
                        android:id="@+id/projectCreatorUniversity"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="University of Alberta"
                        android:textColor="#777777"
                        android:textSize="12sp" />
                </LinearLayout>

                <ImageView
                    android:id="@+id/projectCreatorAvatar"
                    android:layout_width="32dp"
                    android:layout_height="32dp"

                    android:scaleType="centerCrop"
                    android:background="@drawable/circle_mask"
                    android:contentDescription="Creator Avatar" />
            </LinearLayout>
        </RelativeLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/projectTitleTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="AI Writing Assistant"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#000000" />

        <!-- Description -->
        <TextView
            android:id="@+id/projectDescriptionTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Build an AI-powered writing assistant that helps students..."
            android:textSize="14sp"
            android:textColor="#444444"
            android:maxLines="4"
            android:ellipsize="end" />

        <!-- Tags Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Skills Needed:"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginTop="12dp" />

        <!-- Skills (ChipGroup) -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/projectTagsChipGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:singleLine="false"
            app:chipSpacing="8dp"
            app:singleSelection="false" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#EEEEEE"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp" />

        <!-- Like/Dislike Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/dislikeButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_dislike_border"
                android:src="@drawable/ic_thumb_down"
                android:scaleType="centerInside"
                android:tint="#444444"/>

            <View
                android:layout_width="32dp"
                android:layout_height="1dp" />

            <ImageButton
                android:id="@+id/likeButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_like_border"
                android:src="@drawable/ic_thumb_up"
                android:scaleType="centerInside"
                android:tint="#444444"/>
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
